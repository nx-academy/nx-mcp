services:
  nx-ai:
    build:
      context: .
      target: vim-dev
    env_file:
      - ./.env
    tty: true
    stdin_open: true
    ports:
      - 8000:8000
    volumes:
      - ./:/workspaces/nx-ai
      - ./.vim/.vimrc:/root/.vimrc

